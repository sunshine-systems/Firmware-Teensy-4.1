# Deployed mirrors (vendor Teensy core files copied to top level by deploy.bat)
/cores/
/libraries/
/boards.txt
/platform.txt
/keywords.txt
/installed.json

# One-time pristine backup zip
/teensy_core_backup.zip

# IDE
/.vscode/

# Third-party test clients pulled in by tests/ — they have their own licenses
# and upstream lifecycles, must never enter our history.
/KMBox Net Translator/tests/unibot-kmbox/
/KMBox Net Translator/tests/vendor-demos/
